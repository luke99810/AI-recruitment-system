# 交接：界面重构（已完成）+ 后续待办

> 2026-08-05 更新 · 上一版是"待做"清单，本版记录已做完的部分与剩下的事
> ✅ 已提交并推送：`4a00828 refactor(ui): 视图层重构`

---

## 一、任务背景

考核任务要求见 `C:\Users\宿心\Desktop\任务要求文档.md`。评分权重：

```
Harness 15% + Loop 15% + Graph 10% + Skills 10%  = 50%（核心区分维度）
系统完整性 25% · 创新与深度 10%
代码与文档质量 5% · Demo 易用性与美观度 5% · 飞轮/RAG 5%
```

---

## 二、本轮（2026-08-05）做完的三件事

### 1. ✅ 界面重构 —— 视图层拆分 + 结论先行

`main.py` **2156 → 784 行**。

```
app/ui/theme.py       设计 token + 全局样式（唯一样式来源）
app/ui/components.py  页头/结论横幅/指标网格/评分条/题卡/证据列表/空状态/步骤条/分数环
app/views/analysis.py · interview.py · report.py
```

结果页从「统计→分数环→维度条→优势→差距→风险→题库→追问→链接→Checker→Graph→飞轮」
一路平铺，改为四层收敛：**结论+下一步动作 → 依据 → 明细折叠 → 工程细节折叠**。

顺带修掉的实际问题（都不是审美，是坏的）：
- 主按钮文字不可见 —— Streamlit 把文字包在内层 `<p>/<div>`，
  全局 `p{color:var(--text-2)}` 盖掉了按钮自己的 color，**必须给后代元素上色**
- 禁用态与可用态外观相同
- 侧边栏 footer 用 `position:fixed`，压在 Skills 列表上
- 报告页雷达图用深色底配色（网格 `#2d3245`），而主题锁亮色 → 白底上看不清
- 面试对话改用原生 `st.chat_message`（原来手搓气泡塞进固定高度 div，
  长回答截断到 500 字且不能滚动）
- `.streamlit/config.toml` 被 `.gitignore` 整目录忽略 —— 主题锁定设置在里面，
  不入库别人克隆下来渲染就不一样

### 2. ✅ Checker 结构性永不 PASS（这是本轮最重要的发现）

`维度覆盖` 查 `output["questions"]`，`数据准确性` 查 `matched_points/gap_points`。
但 `match_result` 结构上没有 questions、`questions_output` 结构上没有 matched_points，
两者恒为 0 分，而 `overall_pass` 要求全部维度达标 →
**两条校准链都永远不可能 PASS**。

指纹是 `int(passes / max(checks, 1) * 100)` 里的 `max(...,1)`：
防了除零，却把"没什么可查"变成了"查了，0 分"。**崩溃防对了，默认值取错了。**

修法：`DIMENSION_APPLICABILITY` 按 output_type 判定适用性，不适用的维度
**既不计 0 也不计 100**（计 100 等于伪造通过），排除在判定与加权之外，
并在 `CheckerResult.skipped_dimensions` 显式标注。加权分按参与维度归一化。

修复后实测：试题链**第 1 轮即 PASS**；匹配链的数据准确性在 75→87→80 真实波动。

### 3. ✅ 单次分析 273.9s → 154.9s（−43%，实测）

| 环节 | 修复前 | 修复后 |
|---|---|---|
| 解析 JD+简历（已并行） | 10.3s | 11.8s |
| 匹配评分 | 7.4s | 10.4s |
| 试题生成 | 61.5s | 69.9s |
| 追问 ∥ Skills | 35.2s | 46.2s |
| **Checker 校准循环** | **~159s** | **~17s** |
| **合计** | **273.9s** | **154.9s** |

三处改动：Skills 并行（原来 for 循环串行调 LLM，3 个 Skill 就是 3 次往返首尾相接）·
两条校准链并行（先把 match_result 快照给试题链，否则会读到匹配链的中间态）·
消除无效重生成（即上面第 2 条，贡献最大）。

---

## 三、⚠️ 仍然没做的（按重要性排序）

### 1. Skills 生命周期在 UI 上不完整 —— 直接影响交付物

任务要求 C2 要 List / Insert / Activate / Compose / Delete / Hot Reload 六个操作，
且交付物明确写着演示视频要**重点展示「Skills 插入/激活/删除操作」**。

现状：`app/skills/registry.py` 里六个方法**都实现了**
（`insert` / `delete` / `activate` / `deactivate` / `hot_reload` / `list_all`），
但侧边栏面板只接了 **激活/停用**，`insert` 和 `delete` 没有入口。
`integration.py:72` 有一个「➕ 自定义 Skill」的 expander，需要确认它是否真的调用了
`manager.insert`（我没验证到底）。

**这是演示视频的硬性内容，优先做。**

### 2. 飞轮没有真正用向量数据库

`chromadb` **没装**（requirements.txt 里是注释掉的可选项），
`flywheel.py:88` 走的是 TF-IDF 风格关键词匹配降级路径。
任务 Part D 原文是「存入向量数据库 + RAG 检索」。降级链写得很干净，
但严格说这一项（5% 加分）目前不成立。装 chromadb 或在 README 里
明说当前是降级实现，二选一。

### 3. 自带的三份测试简历全部解析失败

`test_resumes/*.pdf` 三份都是**扫描件/图片型 PDF**，而 `rapidocr-onnxruntime` 未安装，
解析直接抛 `ValueError`。而且 `.gitignore` 里有 `*.pdf`，
所以**克隆下来的仓库连这三份都没有**。

已补 `test_resumes/示例简历-王思远-后端开发.txt`（纯文本，可直接用），
上传页也加了提示。但如果要保留 PDF 演示路径，得把 OCR 依赖装上。

### 4. Checker 修订闭环没跑过真实 LLM 全链路的效果验证

逻辑闭环已经验证（校验→反馈→修订→再校验，交出得分最高轮），
但「修订 prompt 到底有没有让输出变好」只在匹配链上看到 75→87→80 的波动，
样本太少。跑几次真实分析确认修订方向是否稳定。

### 5. 其他

- `app/xtts_offline.py` 325 行是死代码（`SDK/` 目录不存在），删不删由你定
- `generate_questions` 单节点 70s 是剩余最大瓶颈。可按维度拆成 5 个并行调用，
  但那会改变 Graph 拓扑 —— 任务推荐的形状是 `QuestionGen → Ambiguity` 串行，
  为省时间偏离规定拓扑不划算，我没动
- 演示视频还没录

---

## 四、验证方式

```bash
cd "D:\AI recruitment system"
.venv\Scripts\python.exe run.py       # ★ 是 python run.py，不是 streamlit run
                                      #   run.py 自己会拉起 streamlit（8501）
```

**改了 `app/ui/` 或 `app/views/` 必须重启进程** —— Streamlit 只重跑主脚本，
不会重新导入已加载的子模块，改了样式不重启会看不到变化（这个坑我踩了一次）。

### 离线冒烟（不消耗 LLM 额度，推荐）

`streamlit.testing.v1.AppTest` 可以注入 session_state 后直接跑页面，
专门用来抓"某字段缺失时结果页炸掉"这类问题。本轮用它验证了六个页面状态。

### 界面改动必须用浏览器实际打开看

这个项目上踩过的亏：
- 只读代码判断"样式没问题"，实际 60+ 处 CSS 声明是死的
- 把**截图工具的裁剪边界**当成页面溢出 —— 用
  `document.documentElement.scrollWidth > window.innerWidth` 量才发现根本没溢出

### 三条别推翻的实测结论

- **主题必须锁亮色**：Streamlit 既不跟随系统暗色，也不通过 CSS 变量暴露主题
  （`matchMedia` 为 true 但 `.stApp` 背景是 `rgb(255,255,255)`，
  `--background-color` 等全为空串）。用 `prefers-color-scheme` 会导致
  "深色卡片浮在白页上"
- **主导航必须继续用 `st.radio`**：`st.tabs()` 无法用代码切换，
  而"查看评估报告"等按钮需要跳页。问题从来不是选错组件，是它默认长得像单选框 ——
  已用 CSS 改造成分段控件
- **`theme.py` 里不能用 `%` 或 `.format()` 插值**：CSS 里到处是 `100%` 和 `{`，
  两种插值语法都会把它们当占位符（实测报 `not enough arguments for format string`）。
  用哨兵串 `/*__TOKENS__*/` 替换
