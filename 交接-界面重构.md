# 交接：界面重构 + 后续改进记录

> 记录已完成的工作、结论性的实测发现，以及仍未做的事。

---

## 一、任务背景

考核任务要求见 `C:\Users\宿心\Desktop\任务要求文档.md`。评分权重：

```
Harness 15% + Loop 15% + Graph 10% + Skills 10%  = 50%（核心区分维度）
系统完整性 25% · 创新与深度 10%
代码与文档质量 5% · Demo 易用性与美观度 5% · 飞轮/RAG 5%
```

---

## 二、已完成

### 1. 界面重构 —— 视图层拆分 + 结论先行

`main.py` **2156 → 784 行**。

```
app/ui/theme.py       设计 token + 全局样式（唯一样式来源）
app/ui/components.py  页头/结论横幅/指标网格/评分条/题卡/证据列表/空状态/步骤条/分数环
app/views/analysis.py · interview.py · report.py · skills_admin.py
```

结果页从「统计→分数环→维度条→优势→差距→风险→题库→追问→链接→Checker→Graph→飞轮」
一路平铺，改为四层收敛：**结论+下一步动作 → 依据 → 明细折叠 → 工程细节折叠**。

顺带修掉的实际问题（都不是审美，是坏的）：
- 主按钮文字不可见 —— Streamlit 把文字包在内层 `<p>/<div>`，
  全局 `p{color:var(--text-2)}` 盖掉了按钮自己的 color，**必须给后代元素上色**
- 禁用态与可用态外观相同
- 侧边栏 footer 用 `position:fixed`，压在 Skills 列表上
- 报告页雷达图用深色底配色（网格 `#2d3245`），而主题锁亮色 → 白底上看不清
- 面试对话改用原生 `st.chat_message`（原来手搓气泡塞进固定高度 div，
  长回答截断到 500 字且不能滚动）
- `.streamlit/config.toml` 被 `.gitignore` 整目录忽略 —— 主题锁定设置在里面，
  不入库别人克隆下来渲染就不一样

### 2. Checker 结构性永不 PASS

`维度覆盖` 查 `output["questions"]`，`数据准确性` 查 `matched_points/gap_points`。
但 `match_result` 结构上没有 questions、`questions_output` 结构上没有 matched_points，
两者恒为 0 分，而 `overall_pass` 要求全部维度达标 →
**两条校准链都永远不可能 PASS**。

指纹是 `int(passes / max(checks, 1) * 100)` 里的 `max(...,1)`：
防了除零，却把"没什么可查"变成了"查了，0 分"。**崩溃防对了，默认值取错了。**

修法：`DIMENSION_APPLICABILITY` 按 output_type 判定适用性，不适用的维度
**既不计 0 也不计 100**（计 100 等于伪造通过），排除在判定与加权之外，
并在 `CheckerResult.skipped_dimensions` 显式标注。加权分按参与维度归一化。

### 3. 单次分析 273.9s → 154.9s（−43%）

三处改动：Skills 并行 · 两条校准链并行（先把 match_result 快照给试题链，
否则会读到匹配链的中间态）· 消除无效重生成（即上面第 2 条，贡献最大，
Checker 校准循环 ~159s → ~17s）。

### 4. Skills 六个生命周期操作全部有 UI 入口

`app/views/skills_admin.py` 独立页：列出 / 安装 / 激活 / 组合 / 卸载 / 热重载。
此前只有激活/停用两个，而交付物明确要求演示视频展示「插入/激活/删除」。

### 5. 飞轮接入向量检索

`chromadb` 进 requirements。**但默认不走它** —— 见下方「已知限制」。

### 6. 语音与 i18n

修了播放静默失败、讯飞假可用、语音输入结果丢失；候选人端补齐面试官发声与数字人；
界面语言切换真正生效。

### 7. 扫描件 PDF 解析（OCR）

`test_resumes/` 三份 PDF 都是**图片型扫描件**（实测：2 页、0 文本、0 字符对象、2 张图像），
此前一律解析失败。现已可解析，同时把 OCR 结果作为 `.txt` 一并入库
（`.gitignore` 忽略 `*.pdf`，克隆下来本来一份测试简历都没有）。

关键发现：**`onnxruntime` 必须在 `import fitz` 之前载入。**
PyMuPDF 一被 import 就把系统的 `msvcp140.dll` 拉进进程，DLL 在同一进程内按名字
唯一、谁先载入谁生效；某些 Windows 上 System32 那份停在旧版（实测 14.00.24215.1，
而同机 `vcruntime140_1.dll` 已是 14.50），onnxruntime 需要 ≥14.4x，于是抛
`DLL load failed while importing onnxruntime_pybind11_state`，整条 OCR 降级路径静默失效。
实测只有 fitz 触发，pdfplumber / cv2 / numpy 都不会。`app/parser.py` 顶部的 import
顺序是刻意的，改之前先读注释。

### 8. 删掉 `checker.py::run_calibration_loop`

全仓零调用，真正在跑的是 `pipeline.py::_run_checker_loop`。
它比普通死代码危险：名字更像正统实现、位置在 checker 模块里更显眼，却带着
**「三轮未过时返回最后一轮」**的 bug —— 而实测（见下）证明最后一轮经常不是最好的。
删除处留了注释防止被重建。

---

## 三、校准闭环的实测结论

`scripts/verify_calibration.py` 跑真实分析，打印每条链的逐轮加权分。

5 次真实分析（两批：3 次 + 2 次）的结果：

| 链 | 逐轮分 | 结果 |
|---|---|---|
| match_result | `[96.9, 92.8, 96.2]` | 三轮未过，**最高第 1 轮** |
| match_result | `[100.0]` | 第 1 轮 PASS |
| match_result | `[95.9, 95.3, 100.0]` | 第 3 轮 PASS，**修订改好了** |
| match_result | `[96.9, 95.9, 93.4]` | 三轮未过，**逐轮变差**，最高第 1 轮 |
| match_result | `[96.9, 100.0]` | 第 2 轮 PASS，**修订改好了** |
| questions_output | `[84.0]` | 未过但只有 1 轮 → 修订中断（见「已知限制」2） |
| questions_output | `[100.0]` ×3 | 第 1 轮 PASS |

**match_result 进入修订的 4 个样本：**

| 指标 | 结果 |
|---|---|
| 修订后分数上升 | **2 / 4** |
| 最后一轮恰好是最高分 | **2 / 4** |

三条结论：

1. **修订方向是对的，但只有一半概率变好。** 2 个改好（95.9→100、96.9→100），
   2 个改坏（96.9→96.2、96.9→93.4，后者逐轮单调下降）。
   模型按反馈改 A 问题时把 B 改坏是真实且高频的。
2. **择优是必要的，不能返回最后一轮。** 4 个样本里有 2 个"最后一轮不是最好"，
   即**一半情况下择优真的救回了结果**。`pipeline.py::_calibrate` 的
   best_output/best_score 不能去掉 —— 这也印证了第 8 条为什么要删
   `run_calibration_loop`（它正是返回最后一轮）。
3. **questions_output 链几乎总是首轮 PASS**（4 次里 3 次），
   说明它的维度判定已经够宽松，修订链路基本用不上。

> 样本量仍然小（match_result 只有 4 个进入修订的样本），
> 「一半变好一半变差」这个比例还需要更多样本才能当作稳定结论。
> 但「最后一轮不一定最好」这一条已经被 2 个反例坐实，足够支撑择优的必要性。

---

## 四、⚠️ 仍然没做 / 已知限制

### 1. 飞轮默认不走 chromadb

`chromadb` 装了，但默认走**内置向量索引**（纯 Python，n-gram 哈希 embedding +
余弦相似度）。原因写在 `requirements.txt`：Python 3.13 + Windows 上实测
chromadb 1.5.9 会在 `collection.add` 处**段错误**，而段错误直接杀进程、
`try/except` 拦不住；降到 0.5.x 又没有 3.13 的预编译轮子。

要启用真 HNSW：`FLYWHEEL_VECTOR_BACKEND=chroma`，且**环境需 Python 3.11/3.12**。
本机是 3.13.14，所以默认关闭。演示若要体现「向量数据库 + RAG」这一项（5% 加分），
得换 3.11/3.12 环境，或在说明里讲清当前是内置实现。

### 2. 试题链的修订会失败

实测出现过一次「三轮未过但只有 1 轮记录」——`_calibrate` 里 `regenerate()` 抛异常后
`break`。此前该异常**只 append 进 execution_log、不打日志**，界面和终端都看不见，
表现为"轮次莫名其妙少了"。现已补 `logger.warning`，并区分"返回空"和"抛异常"两种。

**但根因仍未定位**：补完日志后又跑了 2 次，questions 链两次都第 1 轮就 PASS，
没再进入修订，所以异常没复现。怀疑是 `HarnessConfig(timeout_seconds=90)` 太紧
——试题生成正常就要 ~70s，修订版 prompt 更长，容易压线超时。

下次若再出现「某条链轮次异常少」，直接看输出里的 `[log] revision_failed`，
里面现在带异常类型和消息。

### 3. `generate_questions` 单节点 ~70s 是剩余最大瓶颈

可按维度拆成 5 个并行调用，但会改变 Graph 拓扑 —— 任务推荐的形状是
`QuestionGen → Ambiguity` 串行，为省时间偏离规定拓扑不划算，**刻意没动**。
若要动，先确认评分标准对拓扑的要求。

### 4. `app/xtts_offline.py` 不是死代码，别删

`app/tts_utils.py:271` 有 `from .xtts_offline import XTTSOffline`。
它跑不起来（`SDK/libs/64/` 不存在），但 `engine_status()` 已经如实报告
「缺少本地 SDK」，调用路径也 try/except 降级到 edge-tts。
**当前状态是正确的**，删掉反而要连带改 tts_utils 和两份文档。

---

## 五、验证方式

```bash
cd "D:\AI recruitment system"
.venv\Scripts\python.exe run.py       # ★ 是 python run.py，不是 streamlit run
                                      #   run.py 自己会拉起 streamlit（8501）
```

**改了 `app/ui/` 或 `app/views/` 必须重启进程** —— Streamlit 只重跑主脚本，
不会重新导入已加载的子模块，改了样式不重启会看不到变化。

### 离线冒烟（不消耗 LLM 额度，推荐）

`streamlit.testing.v1.AppTest` 可以注入 session_state 后直接跑页面，
专门用来抓"某字段缺失时结果页炸掉"这类问题。

### 校准闭环验证（消耗额度）

```bash
.venv\Scripts\python.exe scripts\verify_calibration.py 3
```

### 界面改动必须用浏览器实际打开看

这个项目上踩过的亏：
- 只读代码判断"样式没问题"，实际 60+ 处 CSS 声明是死的
- 把**截图工具的裁剪边界**当成页面溢出 —— 用
  `document.documentElement.scrollWidth > window.innerWidth` 量才发现根本没溢出

### 四条别推翻的实测结论

- **主题必须锁亮色**：Streamlit 既不跟随系统暗色，也不通过 CSS 变量暴露主题
  （`matchMedia` 为 true 但 `.stApp` 背景是 `rgb(255,255,255)`，
  `--background-color` 等全为空串）。用 `prefers-color-scheme` 会导致
  "深色卡片浮在白页上"
- **主导航必须继续用 `st.radio`**：`st.tabs()` 无法用代码切换，
  而"查看评估报告"等按钮需要跳页。问题从来不是选错组件，是它默认长得像单选框 ——
  已用 CSS 改造成分段控件
- **`theme.py` 里不能用 `%` 或 `.format()` 插值**：CSS 里到处是 `100%` 和 `{`，
  两种插值语法都会把它们当占位符（实测报 `not enough arguments for format string`）。
  用哨兵串 `/*__TOKENS__*/` 替换
- **`app/parser.py` 的 import 顺序不能调**：onnxruntime 必须在 fitz 之前，
  否则扫描件 OCR 静默失效（见第二节第 7 条）
