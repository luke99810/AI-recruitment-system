# 交接：界面重构（下一轮从这里开始）

> 2026-08-04 · 给新会话的上下文恢复文档
> ⚠️ **当前所有改动都未提交**（`git status` 有 9 个文件变更）。先看 diff 再动手。

---

## 一、先读这个：任务背景

考核任务要求见 `C:\Users\宿心\Desktop\任务要求文档.md`。评分权重：

```
Harness 15% + Loop 15% + Graph 10% + Skills 10%  = 50%（核心区分维度）
系统完整性 25% · 创新与深度 10%
代码与文档质量 5% · Demo 易用性与美观度 5% · 飞轮/RAG 5%
```

**界面只占 5%**，但它是评审的第一眼，也是演示视频的主体——而演示视频是硬性交付物
（红线：无可验证 PoC/日志/视频 → 直接淘汰）。

---

## 二、已完成（都经过实测验证，不是"写完了"）

| 模块 | 做了什么 | 验证方式 |
|---|---|---|
| **Graph** | 消掉全局 `data_pool`，改显式数据边（11 条）+ provenance；条件分支从"只写日志"改为真的影响执行；上游失败 → 下游 SKIPPED | 5 项冒烟：并行分层、未声明 key 不泄漏、低分走补材料分支、失败传播、Mermaid 带数据流 |
| **Harness** | 补齐超时 / 指数退避 / temperature 真传给 agent / Level 4 降级输出 / 指标真落地 | 实测：1s 超时 1.0s 返回、间隔 0.3→0.6、temperature [0.3, 0.1] |
| **Checker** | `run_calibration_loop` 原本**零调用**；补真闭环：校验→反馈→修订→再校验，3 轮不过标记 degraded 并交出**得分最高轮**（不是最后一轮） | 3 组冒烟，含"30/88/55 三轮应交出 88 那轮" |
| **语音** | 引擎可用性探测（不可用的不进下拉框）+ 失败带原因不再静默 | 实测 XTTS 因缺 SDK 被正确排除并降级 |
| **CSS token** | 补齐 60+ 处引用但未定义的变量；替换 29 处不存在的 `--st-color-*` | AST 检查 + 浏览器实测 |
| **i18n** | `app/i18n.py` 中英双语，导航状态与语言解耦 | 切换实测不掉页；`missing_report()` 零缺失 |
| **设置页** | `app/settings_page.py` 模型 API 配置 + 语言 + 引擎状态 | 浏览器实测，Key 掩码显示 |

---

## 三、⚠️ 下一轮要做的：三个主页面的布局重构

**这是唯一没做的部分。** 前面都是结构和地基，视图层的信息架构没动。

### 现状问题（浏览器实测过的，不是猜的）

1. `app/main.py` **1932 行**，UI 逻辑与业务判断混在一起，
   `tab_resume_analysis` / `tab_ai_interview` / `tab_report` 各自几百行
2. 三个页面的 `.app-header` 是复制粘贴的三份，含一大坨内联 base64 logo
3. 结果展示（`_show_analysis_results`，260+ 行）是长条堆叠，没有信息层级
4. 侧边栏底部 Skills 库排版松散
5. `"或"` 分隔符渲染成一个突兀小方块

### 建议做法

- **先拆文件**：`views/analysis.py` / `views/interview.py` / `views/report.py` + `components/`
- **抽公共组件**：page_header / stat_card / score_ring / question_card / empty_state
- **信息架构**：结果页应该是"结论先行"——匹配分 + 建议在最上，证据和明细折叠
- **所有文案走 `t()`**，i18n 字典已建好，加 key 即可

### 已定的设计基础（别推翻）

- 设计 token 在 `main.py` 的 `<style>` 段，语义化命名（`--surface` / `--text-2` / `--border`）
- **主题锁定亮色**，理由见 `.streamlit/config.toml` 里的说明——
  实测过 Streamlit 既不跟随系统暗色，也不通过 CSS 变量暴露主题，
  用 `prefers-color-scheme` 会导致"深色卡片浮在白页上"
- 主导航**必须继续用 `st.radio`**，不能换 `st.tabs()`——
  tabs 无法用代码切换，而"查看评估报告"等按钮需要跳页

---

## 四、验证方式（沿用，别只看代码）

```bash
cd "D:\AI recruitment system"
.venv\Scripts\python.exe run.py       # ★ 是 python run.py，不是 streamlit run run.py
                                      #   run.py 自己会拉起 streamlit（8501）
```

**界面改动必须用浏览器实际打开看**。这轮踩过两次亏：

- 一次是只读代码判断"样式没问题"，实际 60+ 处声明是死的
- 一次是把**截图工具的裁剪边界**当成页面溢出——用 JS 量了才发现
  `scrollWidth == innerWidth`，根本没有横向溢出

要量真实布局用：
```js
document.documentElement.scrollWidth > window.innerWidth   // 是否真溢出
[...document.querySelectorAll('.stApp *')].filter(e => e.getBoundingClientRect().right > innerWidth+1)
```

---

## 五、未闭环 / 需注意

- **所有改动未提交**，也未推远端
- `app/xtts_offline.py` 325 行是死代码（`SDK/` 目录不存在），是否删由你定
- Checker 的修订闭环**只用假 Agent 验过逻辑**，没跑过真实 LLM 全链路——
  跑一次真实分析确认修订 prompt 的效果，是下一轮该做的
- README 还没更新到 v2.2 的四层架构 + 新增的设置页/i18n
